---
const { textButton, colorBg, borderBg, hoverBg } = Astro.props;
---

<button
    class=`px-6 py-3 text-white rounded-3xl font-semibold border-b-2 ${colorBg} border-${borderBg} hover:bg-${hoverBg}`
    >{textButton}</button
>
